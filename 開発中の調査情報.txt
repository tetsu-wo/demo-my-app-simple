チェックすべき「犯人」の探し方
この現象が起きる原因は、大きく分けて2つです。

1. セキュリティグループ（門番）の閉鎖
ALBからの「生きてる？」という通信（curl）を、コンテナ側のセキュリティグループがブロックしている。

2. アプリの応答ミス
アプリは起動しているが、ALBが期待しているパス（/など）で 200 OK を返していない（404や500になっている）。

3. そもそもアプリがクラッシュしている
コンテナは「実行中」に見えても、中のプログラムがエラーで即座に終了しようとしている（再起動を繰り返している）。